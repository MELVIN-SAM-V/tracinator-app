# Shell scripts must keep LF endings — a CRLF shebang line (e.g. from a
# Windows checkout with core.autocrlf=true) makes `/usr/bin/env bash` fail
# with "bash\r: No such file or directory".
*.sh text eol=lf
